<div class='container add-font'>
  <div class='row'>
    <div class="col-md-8 mx-auto my-2">
      <h2 class="my-5"><mark class="bg-light">商品詳細</mark></h2>
      <div class='row'>
        <div class="col-md-6 mx-auto">
          <%= image_tag @item.get_image(280,300) %>
        </div>
        <div class="col-md-6 mx-auto">
          <h5>| 商品名</h5>
            <p><%= @item.name %></p>
          <h5>| 商品説明</h5>
            <p><%= @item.description %></p>
          <h5>| ジャンル</h5>
            <p><%= @item.genre.name %></p>
           <!-- モデルに記述したadd_tax_priceメソッドを使用して税込価格を表示 -->
             <!-- (:dalimited)は数値を3桁区切りにしてくれる(ex:1,234円) -->
          <h5>| 税抜価格（税込価格）</h5>
            <p><%= @item.price.to_s(:delimited) %> ( <%= @item.add_tax_price.to_s(:delimited) %> )円</p>
           <!-- もしステータスがtrueであれば販売中を返し、そうでなければ販売停止中を返す -->
          <h5>| 販売ステータス</h5>
          <div>
            <% if @item.is_active == true %>
              <p class="font-weight-bold text-success">販売中</p>
            <% else %>
              <p class="font-weight-bold text-secondary">販売停止中</p>
            <% end %>
          </div>
        </div>
      </div>
        <div class="col-12 text-center mt-4">
          <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-outline-success" %>
        </div>
    </div>
  </div>
</div>
